#if os(iOS)
// AuthCoordinatorView.swift
// Superseded by GlacialLoginScreen + FirstRunCoordinatorView (Wave 12 / Design Review).
// File kept as a stub to avoid breaking git history.
import SwiftUI
#endif
