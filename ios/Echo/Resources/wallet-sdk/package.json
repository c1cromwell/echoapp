{
  "name": "echo-wallet-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Self-contained dag4 wallet bundle for embedding in iOS JavaScriptCore (Echo real-custody signing).",
  "scripts": {
    "build": "node build.js",
    "vector": "node gen-vector.js"
  },
  "dependencies": {
    "@stardust-collective/dag4-keystore": "2.8.1",
    "buffer": "6.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "esbuild-plugin-polyfill-node": "^0.3.0"
  },
  "comment-overrides": "These are eth-lib's unused networking transitive deps (servify/request/ws). They are tree-shaken out of echo-wallet.bundle.js; the overrides also clear them from the dependency tree / npm audit.",
  "overrides": {
    "form-data": "^4.0.4",
    "ws": "^8.18.0",
    "qs": "^6.14.1",
    "tough-cookie": "^4.1.3",
    "elliptic": "^6.6.1",
    "secp256k1": "^5.0.1"
  }
}
